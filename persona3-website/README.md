# Persona 3 Style Personal Website

The original Drive asset link appears to be empty:

https://drive.google.com/drive/folders/1_dVxHVTJO_YtMAo9ia7wnja1XY0nImr5?usp=drive_link

The usable assets for this project are checked into `src/assets/` instead. The current build uses `main1.mp4` as the shared animated background because the README-referenced folder does not contain the missing `Mainn.mp4`, `main2.mp4`, or `main3.mp4` clips.

## Local Setup

```sh
npm install
npm run dev
```

Vite 8 requires Node 20.19+ or 22.12+. If your local `node -v` is older, upgrade Node or run with a newer Node on your PATH.
